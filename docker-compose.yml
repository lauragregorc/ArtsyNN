services:
  jupyter:
    image: artsy_jupyter
    ports:
      - 7777:8888
    environment:
      - JUPYTER_TOKEN=artsy
      - GRANT_SUDO=1
    volumes:
      - ./src:/src
    restart: unless-stopped
    entrypoint: sh -c 'jupyter lab --notebook-dir=/src --allow-root'
